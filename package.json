{
  "name": "sample-banking-demo-monorepo",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "all-start": "pnpm recursive run --workspace-concurrency 400 --stream=true start",
    "all-simulate-traffic": "pnpm recursive run --workspace-concurrency 400  --stream=true simulate-traffic"
  },
  "workspaces": [
    "packages/*"
  ],
  "keywords": [],
  "author": "",
  "license": "ISC"
}
